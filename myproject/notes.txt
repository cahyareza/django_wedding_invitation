                <form action="{% url 'order:update' order.id %}" method="POST" enctype="multipart/form-data" novalidate>
                    {% csrf_token %}
                    <!--   INFORMASI PEMBAYARAN  -->
                    <div class="notification is-light p-1 has-text-centered mt-4">
                        INFORMASI PEMBAYARAN
                    </div>
                    <div class="columns is-multiline mt-1">
                        <div class="column is-8 mb-">
                            {{ orderform.bukti.label_tag }}
                            {{ orderform.bukti }}

                            {% for error in orderform.bukti.errors %}
                                <p class="help is-danger">{{ error }}</p>
                            {% endfor %}
                        </div>
                    </div>
                    <button id="submit" type="submit" class="button is-light p-2 mt-1">Submit</button>
                </form>